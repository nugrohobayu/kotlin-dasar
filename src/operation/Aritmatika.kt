package operation

fun main() {
    val a = 10;
    val b = 5;
    val result = a / b;

    // Augmented Assignment
    // a = a + 10       a += 10;
    // a = a - 10       a -= 10;
    // a = a * 10       a *= 10;
    // a = a / 10       a /= 10;
    // a = a % 10       a %= 10;

    var total: Int = 200;
    val barang1 = 1000;
    total += barang1;
    println(total)

    // Unary Operator
    // a = a + 1        a++
    // a = a - 1        a--
    // -                negative
    // +                positive
    // !                Boolean kebalikan

    //operasi perbandingan
    // >                lebih dari
    // <                kurang dari
    // >=               lebih dari samadengan
    // <=               kurang dari samadengan
    // ==               samadengan
    // !=               tidak samadengan


}